import React from 'react';
import { View } from 'react-native';

const Button = () => (
  <View
    style={{
      width: 200,
      height: 200,
      backgroundColor: 'red',
    }}></View>
);

export default Button;
